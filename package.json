{
  "name": "security-sentinel-frontend",
  "version": "1.0.0",
  "description": "Nomadik Security Operations Sentinel Dashboard",
  "main": "index.html",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r index.html _headers *.css *.js assets dist/ 2>/dev/null || cp index.html dist/index.html && cp _headers dist/_headers",
    "lint": "echo 'No linting required for static assets'",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "Nomadik Security Operations",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "resend": "^6.19.0",
    "stripe": "^22.5.0"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}